<td><%= customer.name %></td>
<td><a href="mailto:<%= customer.email %>"><%= customer.email %></a></td>
<td><%= customer.phone %></td>
<td><%= customer.country.name %></td>
<td class="text-right"><%= link_to 'Edit', edit_customer_path(customer), :class => 'btn btn-default btn-xs' %> 
	<%= link_to 'Delete', [customer], :class => 'btn btn-default btn-xs', method: :delete, data: { confirm: 'Are you sure?' } %> <%= link_to 'Contacts', customer_contacts_path(customer), :class => 'btn btn-default btn-xs' %> </td>